<view class="container">
  <view class="page-title">
    {{title}}
  </view>
  <view class="interval-title">
    导师
  </view>
  <view class="avatar-area">
    <view class="avatar-box" wx:for="{{mentors}}"> 
      <image class="avatar" src="{{item.avatar}}"></image>
      <image class="tiny-del" hidden="{{mentorHide}}" data-uid="{{item.uid}}" src="../../img/tinydel.png" bindtap="delMentor"></image>
      {{item.nickName}}
    </view>
    <view class="avatar-box"> 
      <image class="avatar" src="../../img/iconadd.png" bindtap="toMentorAdd"></image>
      添加
    </view>
    <view class="avatar-box"> 
      <image class="avatar" src="../../img/icondel.png" data-key="mentor" bindtap="showDel"></image>
      删除
    </view>
  </view>
  <view class="interval-title">
    队长
  </view>
  <view class="avatar-area">
    <view class="avatar-box" wx:for="{{captains}}"> 
      <image class="avatar" src="{{item.avatar}}"></image>
      <image class="tiny-del" hidden="{{captainHide}}" data-uid="{{item.uid}}" src="../../img/tinydel.png" bindtap="delCaptain"></image>
      {{item.nickName}}
    </view>
    <view class="avatar-box"> 
      <image class="avatar" src="../../img/iconadd.png" bindtap="toCaptainAdd"></image>
      添加
    </view>
    <view class="avatar-box"> 
      <image class="avatar" src="../../img/icondel.png" data-key="captain" bindtap="showDel"></image>
      删除
    </view>
  </view>
  <view class="interval-title">
    参与者
  </view>
  <view class="avatar-area">
    <view class="avatar-box" wx:for="{{members}}"> 
      <image class="avatar" src="{{item.avatar}}"></image>
      <image class="tiny-del" hidden="{{memberHide}}" data-uid="{{item.uid}}" src="../../img/tinydel.png" bindtap="delMember"></image>
      {{item.nickName}}
    </view>
    <view class="avatar-box"> 
      <image class="avatar" src="../../img/icondel.png" data-key="member" bindtap="showDel"></image>
      删除
    </view>
  </view>
  <view class="btn-area">
    <button type="primary" bindtap="toCaptainAdd">添加队长</button>
    <button type="default" bindtap="toMentorAdd">添加导师</button>
  </view>
</view>
